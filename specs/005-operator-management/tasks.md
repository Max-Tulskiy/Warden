# Tasks: Operator management and roles

**Plan:** [./plan.md](./plan.md) · **Status:** done · **Date:** 2026-09-24

> Each task is a verifiable outcome (a test, an endpoint, a screen), not an
> abstract action. Check it off with `[x]` once done.
>
> Within every slice the tests come first and are seen to fail (red) before
> the implementation that makes them pass (green). The API contract is
> regenerated at the end of the server slices; `test_openapi_contract.py`
> compares the checked-in file to `app.openapi()` and stays red between the
> endpoint change and the regeneration, which is kept as its own commit.
> Regenerate with `python scripts/export_openapi.py` from `server/`.

## Foundation

- [x] T-1. Create and switch to the branch `005-operator-management` from `main` (constitution Section IV); `git branch --show-current` prints it

## Slice A — roles and their enforcement (R-1, R-7…R-9, R-12, R-14; A-1…A-3, A-8, A-10, A-11, A-15)

- [x] T-2. Server unit tests, failing first, in `server/tests/unit/test_models/test_operator.py` (with `__init__.py`): storing an `Operator` with no role fails; `status` defaults to active; a stored role and status read back as the enum members; the roles are exactly administrator and observer, the statuses exactly active and disabled
- [x] T-3. Migration test, failing first, in `server/tests/integration/test_migrations/test_operator_roles.py` (with `__init__.py`), against a temporary SQLite file: after upgrading to the previous revision and inserting an operator, upgrading to head gives that operator the role administrator and the status active; the finished `role` column has no server default (so a raw insert without a role fails) while `status` keeps its default; downgrading one revision removes both columns and keeps the operator
- [x] T-4. Server integration tests, failing first, in `server/tests/integration/test_roles/` (with `__init__.py`), using new `viewer` and `viewer_headers` fixtures in `server/tests/conftest.py`: **the permission matrix** in `test_permission_matrix.py` calls every operator endpoint with an administrator token and an observer token and compares the result with the table in plan §1 (observer allowed: `GET /agents`, `/agents/{id}/events`, `/agents/{id}/inventory/changes`, `/events`, `/policy`, `/auth/me`, `POST /auth/password`; observer refused with 403 and administrator not refused: `POST /enrollment-tokens`, `PATCH /agents/{id}`, `POST /agents/{id}/requests`, `GET /audit`, and every `/operators` endpoint); `test_open_endpoints.py` asserts that the operations in `app.openapi()` with neither a bearer security requirement nor an `X-Agent-Key` header are exactly `POST /auth/login`, `POST /enroll`, and `GET /health`; `test_me.py` covers `GET /auth/me` for both roles and 401 without a token; `test_disabled_operator.py` covers a disabled operator's login → 401 with the wrong-password message, an existing token → 401, the audit reason `disabled` for the right password and `bad_password` for a wrong one; `test_live_role.py` covers a role changed directly in the database applying to the same token on its next request, in both directions (A-7, A-8)
- [x] T-5. Add `OperatorRole`, `OperatorStatus`, `role` (no default) and `status` (default active) to `server/src/warden_server/models/operator.py` and a new Alembic revision (down-revision `d23c48cef8fe`) that adds both columns, backfills `role` with a temporary `ADMIN` server default and then drops that default with `batch_alter_table`, and drops both columns on downgrade; T-2 and T-3 pass, and `alembic check` reports no drift
- [x] T-6. Add `require_admin` and the disabled-account refusal to `server/src/warden_server/api/deps.py`; make `create_enrollment_token`, `set_agent_status`, `create_window_request`, and the audit router administrator-only; make `login` refuse a disabled account; add `GET /api/v1/auth/me` with `MeOut`; create the seed operator as an administrator in `services/bootstrap.py`; pass an explicit role in `server/tests/conftest.py`, `server/tests/unit/test_services/test_seed_operator_bootstrap.py`, and `agent/tests/integration/conftest.py`; T-4 passes and the rest of the server and agent suites still pass

## Slice B — account management (R-2…R-6, R-10, R-11, R-13; A-4…A-14, A-18)

- [x] T-7. Move the single-statement `token_version` increment from `server/src/warden_server/api/auth.py` into new `server/src/warden_server/services/sessions.py` and use it from there; the session-revocation tests pass unchanged
- [x] T-8. Server unit tests, failing first, in `server/tests/unit/test_schemas/test_operator_schemas.py`: a username of 1 and of 64 valid characters is accepted, one of 65, an empty one, one starting with a dot, and ones containing a space or a slash are rejected; a password of 11 characters is rejected, 12 and 1024 accepted, 1025 rejected; a role outside the two is rejected; `OperatorUpdateIn` needs at least one of role and status and rejects any other field, including a password
- [x] T-9. Server integration tests, failing first, in `server/tests/integration/test_operators/test_operators.py`: the list shows every account with id, username, role and status, ordered by username, and never any password data; creating an operator returns 201, stores an Argon2id hash that verifies, and audits `operator.created` with the role; a username equal to an existing one ignoring case → 409 and nothing stored; a bad username or a short password → 422 and nothing stored; a role change returns 200, audits `operator.role_changed` with from and to, and does not raise `token_version`; disabling ends the operator's session, refuses their login, raises `token_version`, and audits `operator.disabled`; enabling lets them sign in again and audits `operator.enabled`; setting the value the account already has → 200 and no audit row; `PATCH` with the caller's own id → 409 and nothing changes; a password reset → 204, the old password and the account's session stop working, the new password signs in, a throttled account is unlocked, and `operator.password_reset` is audited; a reset with the caller's own id → 409; an unknown id → 404 on both id endpoints; no token → 401 and an observer token → 403 on all five; two administrators, one disables the other and the other's token is refused; after every operation no audit row and no response contains any password or password hash
- [x] T-10. Add `server/src/warden_server/schemas/operator.py` and `server/src/warden_server/api/operators.py` (router-level `require_admin`, case-insensitive duplicate check, own-id refusal, idempotent changes, throttle reset on a password reset), and register the router in `server/src/warden_server/main.py`; T-8 and T-9 pass
- [x] T-11. Regenerate `contracts/openapi.yaml` for the operators paths, `/auth/me`, and the 403 responses; `test_openapi_contract.py` passes

## Constitution

- [x] T-12. Amend `.specify/memory/constitution.md` to 1.2.0 (MINOR) through the Section VII procedure: a new decision D-10 (two roles, enforced by the server on every request, the role kept in the database and not in the token, and why); three Section V boundaries (an observer sees everything collected; an initial or reset password is known to the administrator who set it and nothing forces a change; two administrators acting on each other at the same instant can leave none active); a version-history row; the updated "last amended" date; the documents made stale by it are those covered by T-25…T-28

## Slice C — panel (R-8, R-12, R-15; A-16, A-17)

- [x] T-13. Client tests, failing first, in `web/tests/unit/apiClient.test.ts`: `getMe` reads `/auth/me`; `listOperators` reads `/operators`; `createOperator` posts `{username, role, password}`; `updateOperator` sends `PATCH /operators/{id}` with only the given fields; `resetOperatorPassword` posts `{new_password}` to `/operators/{id}/password` and resolves on 204; with a handler set by `setForbiddenHandler`, a 403 to a request that carried a token calls it once and still throws `ApiError` with `status === 403`; a 403 without a token, a 401, and a 409 do not call it; a cleared handler is not called
- [x] T-14. Add the calls, the `Role` and `Operator` types, and `setForbiddenHandler` to `web/src/api/client.ts` and `web/src/api/types.ts`; T-13 passes
- [x] T-15. Tests, failing first, in `web/tests/unit/authProvider.test.tsx`: with a token present the provider loads the role with `getMe` and exposes it; with no token the role is null and nothing is requested; a sign-out clears it; the registered forbidden handler reloads the role, so a demotion shows up; a failed `getMe` leaves the role null and does not end the session by itself
- [x] T-16. Add `role` to `web/src/state/authContext.ts` and load and reload it in `web/src/state/auth.tsx` (registering and unregistering the forbidden handler as the unauthorized one is); T-15 passes
- [x] T-17. Tests, failing first, in `web/tests/unit/appShell.test.tsx` and new `web/tests/unit/requireAdmin.test.tsx`: an administrator's sidebar lists «Станции», «Отчёты», «Журнал», «Операторы», «Настройки» in that order and an observer's omits «Журнал» and «Операторы»; the person's role is shown next to their name in Russian; `RequireAdmin` renders nothing while the role is unknown, renders its children for an administrator, and for an observer renders «Недостаточно прав для просмотра этого раздела» inside the shell without touching the session
- [x] T-18. Update `web/src/components/AppShell.tsx`, add `web/src/components/RequireAdmin.tsx`, and add the `/operators` route and the guard on `/audit` in `web/src/App.tsx` (a placeholder page for `/operators` until T-22); T-17 passes
- [x] T-19. Tests, failing first: in `web/tests/unit/agentsPage.test.tsx` the enrollment token button is present for an administrator and absent for an observer; in `web/tests/unit/settingsPage.test.tsx` the «Станции» card is present for an administrator and absent for an observer, while the password, sessions, and policy cards stay for both; add `role: "admin"` to the auth context of every existing test in those two files that relies on the administrator controls; a new `web/tests/unit/agentDetailPage.test.tsx` checks that the window request form is present for an administrator and absent for an observer
- [x] T-20. Hide the enrollment token button in `web/src/pages/AgentsPage.tsx`, the window form in `web/src/pages/AgentDetailPage.tsx`, and the «Станции» card in `web/src/pages/SettingsPage.tsx` for anyone but an administrator; T-19 passes
- [x] T-21. Tests, failing first, in new `web/tests/unit/operatorsPage.test.tsx`: the list shows name, role in Russian, and status for each account, and the person's own row reads «это вы» and offers no action; creating sends `{username, role, password}` and adds the row; a 409 shows «Оператор с таким именем уже существует» and a 422 shows a message about the name or password; changing a role needs «Применить», sends `PATCH` with only the role, and «Отмена» sends nothing; disabling asks for confirmation naming the account, then sends `PATCH` with `disabled`, and enabling needs none; resetting a password needs the new password and a matching repeat, then posts it, and «Отмена» sends nothing; a 403 shows «Недостаточно прав»; a failed load shows «Не удалось загрузить операторов»
- [x] T-22. Add `web/src/pages/OperatorsPage.tsx` (and any table styles in `web/src/styles.css`, existing tokens only) and use it for `/operators`; T-21 passes
- [x] T-23. Tests, failing first, in `web/tests/unit/audit.test.ts`: add `operator.created`, `operator.role_changed`, `operator.disabled`, `operator.enabled`, and `operator.password_reset` to the list of emitted actions, so each needs a distinct Russian name and stays inside the `operator` group
- [x] T-24. Add the five names to `web/src/lib/audit.ts`; T-23 passes

## Documentation

- [x] T-25. Update `docs/ru/architecture.md` (canonical): an authorization section with the table of what each role may do, that the role is read from the database on every request and not stored in the token, the 403-versus-401 distinction, the account endpoints and their rules (own account, case-insensitive names, disabling ends sessions, reset), the open-endpoints test; the «Границы» list gains the three limits; the data-model table describes `role` and `status`
- [x] T-26. Mirror T-25 in `docs/en/architecture.md`, same headings and same statements
- [x] T-27. Update `docs/ru/usage.md`: a «Операторы» section (creating an observer, changing a role, disabling, resetting a password, what an observer sees), the first-administrator note (the seed account is the first administrator and should create a personal one), and the honest-status section extended with what the manual pass did and did not cover
- [x] T-28. Mirror T-27 in `docs/en/usage.md`

## Verification

- [x] T-29. Run `ruff check .`, `ruff format --check .`, `mypy src alembic`, and `bandit -c pyproject.toml -r src` in `server/` — all clean
- [x] T-30. Run the full server suite with `pytest --cov=warden_server --cov-report=xml --cov-fail-under=80` and, once the change is committed, `diff-cover coverage.xml --compare-branch=main --fail-under=80` — green, changed-code coverage at least 80%
- [x] T-31. With the server installed editable (`pip install --no-deps -e server`), run `ruff check .`, `ruff format --check .`, `mypy src`, `bandit -c pyproject.toml -r src`, and `pytest` in `agent/` — all clean and green (no source change; regression run against the real server code)
- [x] T-32. Run `npm run lint`, `npm run format`, `npm test`, and `npm run build` in `web/` — all clean
- [x] T-33. Against real PostgreSQL from `docker compose` (a separate project with fresh volumes and the proxy on non-standard ports, without editing `docker-compose.yml`) run `alembic upgrade head`, `alembic downgrade -1`, `alembic upgrade head`, and confirm an operator row created before the upgrade reads `ADMIN` and `ACTIVE` and that a raw insert without a role is refused
- [x] T-34. Manual pass on that stack, through the proxy and in real browsers with an administrator and an observer signed in side by side: the observer is created in the panel; the observer's sidebar and screens lack the administrator sections and controls; the observer's token is refused (403) on the administrator endpoints directly; a promotion and a demotion apply on the next action; disabling ends the observer's session and refuses their login; an administrator's changes to their own account are refused; a reset password works; the audit screen shows the new actions under their Russian names; record the outcome and its limits in the honest-status section of `docs/ru/usage.md` and `docs/en/usage.md`
- [x] T-35. Repository checks: no `sys.platform`/`platform.system` or platform imports in the touched server files (principle 1); `docs/ru` and `docs/en` have matching headings for the pages changed; everything added under `specs/` is English, apart from UI strings and Russian document headings quoted verbatim to identify them
- [x] T-36. Set **Status** to `done` in `spec.md`, `plan.md`, and this file
