"""Connecting an agent to a server: the configuration window and its command line.

`logic` holds the operations and touches no operating system; `windows` is
the one module that does, in the pattern of `service/windows.py`. Everything a
person sees is in `messages`, in Russian.
"""
